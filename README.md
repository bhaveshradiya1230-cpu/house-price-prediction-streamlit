# House Price Prediction (UI-Based)

## 🔹 Project Overview
This project predicts house prices based on input features like area, number of bedrooms, bathrooms, and location.  
It uses a **Machine Learning model** trained on house price data and provides a **user-friendly UI** to get predictions instantly.

---

## 📁 Project Structure

House_Price_Prediction/
├── app/
│ └── app.py # UI application
├── Dataset/
│ └── house_data.csv
├── Model/
│ ├── modeltrain.py # Model training script
│ ├── house_model.pkl # Trained ML model
│ └── location_encoder.pkl # Encoder for categorical data (like location)
├── screenshots/ # UI / Output screenshots
├── requirements.txt # Project dependencies
└── README.md # Project documentation
---

## ⚙️ Requirements
Python 3.9+ is recommended.  
Install dependencies with:

```bash
pip install -r requirements.txt


